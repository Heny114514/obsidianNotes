# 习题4
建表
```sql
CREATE TABLE S (
    SNO VARCHAR(10) PRIMARY KEY,
    SNAME VARCHAR(50),
    STATUS INT,
    CITY VARCHAR(50)
);

CREATE TABLE P (
    PNO VARCHAR(10) PRIMARY KEY,
    PNAME VARCHAR(50),
    COLOR VARCHAR(20),
    WEIGHT INT
);

CREATE TABLE J (
    JNO VARCHAR(10) PRIMARY KEY,
    JNAME VARCHAR(50),
    CITY VARCHAR(50)
);

CREATE TABLE SPJ (
    SNO VARCHAR(10),
    PNO VARCHAR(10),
    JNO VARCHAR(10),
    QTY INT,
    PRIMARY KEY (SNO, PNO, JNO),
    FOREIGN KEY (SNO) REFERENCES S(SNO),
    FOREIGN KEY (PNO) REFERENCES P(PNO),
    FOREIGN KEY (JNO) REFERENCES J(JNO)
);
```

查询：
1. `SELECT DISTINCT SNO FROM SPJ WHERE JNO = 'J1';`
2. `SELECT DISTINCT SNO FROM SPJ WHERE JNO = 'J1' AND PNO = 'P1';`
3. `SELECT DISTINCT SPJ.SNO FROM SPJ JOIN P ON SPJ.PNO = P.PNO WHERE SPJ.JNO = 'J1' AND P.COLOR = '红';`
4. `SELECT JNO FROM J WHERE JNO NOT IN (SELECT SPJ.JNO FROM SPJ JOIN S ON SPJ.SNO=S.SNO JOIN P ON SPJ.PNO=P.PNO WHERE S.CITY='天津' AND P.COLOR='红');`
5. `SELECT DISTINCT JNO FROM SPJ WHERE PNO IN (SELECT PNO FROM SPJ WHERE SNO='S1') GROUP BY JNO HAVING COUNT(DISTINCT PNO) >= (SELECT COUNT(DISTINCT PNO) FROM SPJ WHERE SNO='S1');`

# 习题5

### 1
```sql
SELECT SNAME, CITY FROM S;
```
### 2
```sql
SELECT PNAME, COLOR, WEIGHT FROM P;
```
### 3
```sql
SELECT DISTINCT JNO FROM SPJ WHERE SNO = 'S1';
```

### 4
```sql
SELECT P.PNAME, SPJ.QTY 
FROM SPJ 
JOIN P ON SPJ.PNO = P.PNO 
WHERE SPJ.JNO = 'J2';
```
### 5
```sql
SELECT DISTINCT SPJ.PNO 
FROM SPJ 
JOIN S ON SPJ.SNO = S.SNO 
WHERE S.CITY = '上海';
```
### 6
```sql
SELECT DISTINCT J.JNAME 
FROM SPJ 
JOIN S ON SPJ.SNO = S.SNO 
JOIN J ON SPJ.JNO = J.JNO 
WHERE S.CITY = '上海';
```
### 7
```sql
SELECT J.JNO 
FROM J 
WHERE J.JNO NOT IN (
    SELECT DISTINCT SPJ.JNO 
    FROM SPJ 
    JOIN S ON SPJ.SNO = S.SNO 
    WHERE S.CITY = '天津'
);
```
### 8
```sql
UPDATE P SET COLOR = '蓝色' WHERE COLOR = '红';
```
### 9
```sql
UPDATE SPJ 
SET SNO = 'S3' 
WHERE SNO = 'S5' AND PNO = 'P6' AND JNO = 'J2';
```
### 10
```sql
DELETE FROM SPJ WHERE SNO = 'S2';
DELETE FROM S WHERE SNO = 'S2';
```
### 11
```sql
INSERT INTO SPJ (SNO, PNO, JNO, QTY) VALUES ('S2', 'P4', 'J6', 200);
```

# 习题9
创建视图（三建工程 J1）
```sql
CREATE VIEW 三建供应 AS 
SELECT SNO, PNO, QTY 
FROM SPJ 
WHERE JNO = 'J1';
```

查询：
### 1
```sql
SELECT PNO, SUM(QTY) AS 总数量
FROM 三建供应
GROUP BY PNO;
```

### 2
```sql
SELECT * FROM 三建供应 WHERE SNO = 'S1';
```